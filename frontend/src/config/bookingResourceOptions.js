// This file is kept as fallback only.
// Real facility data is now loaded from the backend API.
// BACKEND: GET /api/facilities — FacilityController.getAllFacilities()
export const bookingResourceOptions = [];

// Backward-compatible export (older imports).
export const BOOKING_RESOURCE_OPTIONS = bookingResourceOptions;
