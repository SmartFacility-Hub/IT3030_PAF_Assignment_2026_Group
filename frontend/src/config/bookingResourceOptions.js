/**
 * Stand-in options for the booking “Resource” dropdown and admin filter.
 * Replace with data from your resource catalogue API when that module is ready;
 * keep `id` values aligned with `resourceId` in `BookingController` / database.
 */
export const BOOKING_RESOURCE_OPTIONS = [
  { id: 1, name: 'Lecture Hall A — 101' },
  { id: 2, name: 'Computer Lab — B204' },
  { id: 3, name: 'Seminar Room — C12' },
  { id: 4, name: 'Conference Room — Admin 3F' },
];
